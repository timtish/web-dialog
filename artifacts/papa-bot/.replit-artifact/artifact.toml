kind = "web"
previewPath = "/"
title = "Папа-бот"
description = "A warm, low-friction chat companion for an older adult. The first build focuses on a comfortable one-screen conversation experience with large type, gentle prompts, and a small daily weather glance."
version = "1.0.0"
id = "artifacts/papa-bot"
router = "path"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 18995

[services.development]
run = "pnpm --filter @workspace/papa-bot run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/papa-bot", "run", "build" ]
serve = "static"
publicDir = "artifacts/papa-bot/dist/public"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "18995"
BASE_PATH = "/"
